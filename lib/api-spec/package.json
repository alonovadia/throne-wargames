{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && pnpm -w run typecheck:libs",
    "codegen:check": "pnpm run codegen && test -z \"$(git status --porcelain -- ../api-client-react/src/generated ../api-zod/src/generated)\""
  },
  "devDependencies": {
    "orval": "^8.30.0"
  }
}
